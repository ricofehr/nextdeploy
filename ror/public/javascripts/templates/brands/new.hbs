<form {{action "postItem" on="submit"}} class="form-horizontal">
  <legend>{{#if id}}Edit brand {{name}}{{else}}Add new brand{{/if}}</legend>
  <div class="form-group">
    {{#if errorName}}
      <label class="control-label col-sm-2 alert-danger">Name</label>
    {{else}}
      <label class="control-label col-sm-2 alert-success">Name</label>
    {{/if}}
    <div class="col-sm-3">
    {{input type="text" classNames="form-control" value=name placeholder="Brand Title"}}
    </div>
  </div>

  <div class="form-group">
    <div class="control-label col-sm-2">
      <button type="submit" class="btn btn-primary">Submit</button>
    </div>
  </div>
</form>

